/* * Javascript Library*/// DeleteArcfunction DeleteArc(url,message){	if(confirm(message)){		location.href = url;	}}// showPlaginfunction showPlagin(idno){    pc = ('boxOpen' + (idno));    po = ('boxClose' + (idno));    if( document.getElementById(pc).style.display == "none" ) {      document.getElementById(pc).style.display = "block";      document.getElementById(po).style.display = "none";    }    else {      document.getElementById(pc).style.display = "none";      document.getElementById(po).style.display = "block";    }}// pageTop$(function() {	$('#pageTop').click(function() {		$('body,html').animate({scrollTop:0},500);		return false;	});});//ChDspfunction ChDsp(flg,ch){    var obj=document.all && document.all(ch) || document.getElementById && document.getElementById(ch);    if(obj && obj.style){        if(flg){            obj.style.display = "block";        } else {            obj.style.display = "none";        }    }}//検索ボックス固定function scrollPosEvent(nav, navTop, navHeight, winTop) {	//スクロール位置がnavの位置より下だったらクラスfixedを追加	if (winTop > navTop) {		nav.addClass('fixed');		$('#menuShopContent').addClass('fixed-menu');		$('#menuShopContent').css('top', navHeight);	} else {		nav.removeClass('fixed')		$('#menuShopContent').removeClass('fixed-menu');	}}$(function() {	//navの位置	var nav = $('#searchItemBoxContent');	var navTop = nav.offset().top;	var navHeight = nav.innerHeight();	//スクロールするたびに実行	$(window).on("scroll", function () {		scrollPosEvent(nav, navTop, navHeight, $(this).scrollTop());	});		$('body').on('touchmove', function() {		scrollPosEvent(nav, navTop, navHeight, $(this).scrollTop());	});});